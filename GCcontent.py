
eachSeq = {}

with open('rosalind_gc_1_dataset.txt') as dataset:
	for line in dataset:
		# print(line)
		if line[0] == ">":
			if 
			eachSeq[line] = 0

			current = " "
		else:
			if current == " ":
				current = line
			else:
				current = current + line

print(eachSeq)

